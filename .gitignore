# Secrets — written from CI secrets, never committed
espn_credentials.py

# Local SQLite DB (rebuilt each run)
*.db
*.sqlite3

# Transient pipeline outputs (regenerated each run; docs/data/*.json IS committed by CI, this is data/)
data/*.json
!data/crosswalk-overrides.json

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/

# OS / editor
.DS_Store
.vscode/

# Claude Code local session settings (machine-specific; .claude/launch.json IS committed)
.claude/settings.local.json
